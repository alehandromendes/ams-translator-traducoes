-- Extensão do usuário para o mod loader (o CPDD nunca sobrescreve este arquivo).
-- Carrega a camada de tradução PT-BR do Tradutor de Legendas.
return {
    PersonalLoad = {
        "mods.tl_translate.Init",
    },
}
